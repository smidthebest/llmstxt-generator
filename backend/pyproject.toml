[project]
name = "llmstxt-generator"
version = "0.1.0"
description = "Automated llms.txt generator for any website"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
